doctype html
html(lang="en")
  head
    title= pageTitle
    script(type='text/javascript').
      foo = true;
      if (foo) {
         bar(1 + 5)
      }
  body
    h1 Jade - node template engine
    - var n = {1:'one',2:'two',3:'three'}
    each val, index in n
      li= index + ': ' + val


    #container.col
      if youAreUsingJade
        p You are amazing
      else
        p Get on it!
      p.
        Jade is a terse and simple
        templating language with a
        strong focus on performance
        and powerful features.